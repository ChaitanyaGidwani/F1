# Runtime dependencies for the deployed Space. Deliberately smaller than the
# development requirements.txt: no datasets, scikit-learn or pytest, since the
# Space only serves inference. torch is installed separately from the CPU index
# (see Dockerfile).

transformers==4.57.6
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
Pillow>=10.0.0
numpy>=1.24,<3
huggingface_hub>=0.30.0
